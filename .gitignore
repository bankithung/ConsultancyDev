# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
*.egg-info/
dist/
build/
*.log

# Django
db.sqlite3
*.sqlite3
staticfiles/
media/
# Uploaded student documents. `media/` above did NOT cover this one: encrypted
# originals are deliberately stored OUTSIDE the web-served root, so they live in
# their own directory and slipped past the pattern. In production this holds
# passport and certificate scans -- it must never reach a remote.
private_media/
.env
.env.local
.env.production

# Working copies kept while migrations were reorganised. Not source.
_migrations_backup/

# Node
node_modules/
.next/
out/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Misc
*.bak
*.tmp

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/
deploy/webhook_secret
.deploy_test
