# Pinned to exact versions.
#
# These were previously all `>=` with no upper bound, so a fresh install
# resolved Django 6.0 against a codebase written for 5.2 — which silently
# ignored STATICFILES_STORAGE (removed in 5.1) and left static files unhashed.
# Regenerate with `pip freeze` after any deliberate upgrade.

Django==6.0.7
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1

# RS256 JWT signing and Fernet encryption of PII at rest.
cryptography==49.0.0
PyJWT==2.13.0

# Database
psycopg2-binary==2.9.12
dj-database-url==3.1.2

# Serving
gunicorn==26.0.0
whitenoise==6.12.0

# Config
python-dotenv==1.2.2

# Optional cache backend only. channels / channels-redis / daphne were removed
# with the real-time messaging feature.
redis==8.1.0

# Model Context Protocol server (backend/mcp_server). httpx is the HTTP client
# it uses to talk to /api/; uvicorn and starlette arrive as mcp dependencies.
mcp==1.26.0
httpx==0.28.1

# OAuth sign-in for ChatGPT MCP connections.
django-oauth-toolkit==3.4.1
oauthlib==3.3.1
jwcrypto==1.6.0
requests==2.34.2
